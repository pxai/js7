//const valor = prompt("Introduce un número");
const valor = 4;

for (let i = 0; i <= 10; i++) {
    console.log(`${valor} x ${i} = ${valor*i}`);
    // Alternativas: 
    // console.log(valor + " x " + i + " = " + (valor*i));
    // console.log("%d x %d = %d", valor, i, (valor*i));
}
